services:
  backend:
    build: ./backend
    env_file: ./backend/.env
    volumes:
      # Esto mapea la carpeta "data_uploads" de tu PC a "/app/uploads" en el contenedor
      - ./data_uploads:/app/uploads 
    ports:
      - "3000:3000"